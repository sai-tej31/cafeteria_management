<!DOCTYPE html>
<html lang="en">



<head>

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,height=device-height,initial-scale=1.0,minimum-scale=1.0,maximum-scale=1.0">
  <title> College Cafeteria </title>
  <link rel="stylesheet" type="text/css" href="reset.css">
  <link rel="stylesheet" type="text/css" href="home-app.css">


</head>

<body>
  <style>
    body {
      background-color: lightblue;
      background-attachment: fixed;
      background-size: cover;
    }
  </style>
  <section class ="UserOrders">
    <% if @user.orders.delivered_or_confirmed.count != 0 %>
      <% @user.orders.delivered_or_confirmed.each do |order| %>
        <div class= "UserorderDetails">
          <p style="margin-left: 20px;"> <%= order.date %> </p>
          <p style="margin-left: 20px;"> <%= @user.name %> </p>
          <p style="margin-left: 20px;"> <%= link_to "order details", order_path(id: order.id ) %> </p>
          <p style="margin-left: 20px;"> <%= order.status %>  </p>
        </div>
      <% end %>
    <% else %>
      <div class = "Empty-Orders">
        <% if @current_user.role == "customer" %>
          <p> oops! You have never ordered here Click and  order some delicious food
            <%= link_to "click here" , "/menus" %>
          </p>
        <% else %>
          <p>
            No-Orders from <%= @user.name %> !
            <%= link_to "Go Back to Walk-in" , "/menus" %>
          </p>
        <% end %>
      </div>
    <% end %>
  </section>
</body>
